# Crave
The Online Food Ordering Web Application is a responsive front-end project built using HTML, CSS, and JavaScript (ES6+). It allows users to browse menu items, add them to a cart, update quantities, and view a dynamically calculated bill. The project demonstrates DOM manipulation, event handling, and local Storage usage for data persistence.
